{
  "private": true,
  "scripts": {
    "prod": "gulp --production",
    "dev": "gulp watch",
    "watch": "gulp watch",
    "serve:backend": "php artisan serve --host=0.0.0.0 --port=8000",
    "serve:frontend": "gulp watch"
  },
  "devDependencies": {
    "bootstrap-sass": "^3.0.0",
    "gulp": "^3.9.1",
    "laravel-elixir": "^5.0.0",
    "laravel-elixir-browsersync-official": "^1.0.0"
  }
}
